DATABASE_URL=postgresql+psycopg2://chumber:chumber@db:5432/chumber
TEST_DATABASE_URL=postgresql+psycopg2://chumber:chumber@db:5432/chumber_test

JWT_SECRET_KEY=change-this-to-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_DAYS=7

GEMINI_API_KEY=

BOOTSTRAP_ADMIN_USERNAME=admin
BOOTSTRAP_ADMIN_EMAIL=admin@stchumber.local
BOOTSTRAP_ADMIN_PASSWORD=ChangeMe123!

CORS_ORIGINS=http://localhost:5173

# Google Programmable Search (Custom Search JSON API) — used to find a real
# product photo for AI product creation. Free tier: 100 queries/day.
# Without these, AI product creation still works but reports image_status
# "not_configured" instead of inventing an image.
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_ID=
